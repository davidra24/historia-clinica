export interface propsAnnotationModal {
  valueAnnotation: any;
  buttonText: any;
  titleCard: string;
  onSaveAnnotation: any;
  saveText: string;
  readOnly?: boolean;
  disableButton?: boolean;
}
