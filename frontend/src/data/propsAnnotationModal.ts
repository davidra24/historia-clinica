export interface propsAnnotationModal {
  annotation: any;
  buttonText: any;
  titleCard: string;
  onSaveAnnotation: any;
  saveText: string;
  readOnly?: boolean;
  disableButton?: boolean;
  subTitleCard?: string;
  isCallback?: boolean;
}
